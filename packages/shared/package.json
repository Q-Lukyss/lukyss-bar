{
  "name": "@lukyss-bar/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "dev": "tsc -w -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"shared: lint later\"",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@lukyss-bar/typescript-config": "*",
    "rimraf": "^6.0.0",
    "typescript": "^5.0.0"
  }
}
